create a webapp using vanilla js,html,css,modern threejs,mediapipe.如果mediapipe来自CDN,要用常规的<script>方式引入变量。

实现一个星云情绪卡片功能,内置100个好的四字词语,类似（未来可期、天天开心、按时吃饭）等，并在每个词语下面附带一句话，类似（即使是生活是无底深渊，下去，也是前程万里）等；
通过摄像头识别人的手势，如果五指张开，则星云缓慢转动，如果握拳，则随机抽取一张进行展示，如果再次五指张开，则先做一个卡片变成烟花消失的特效，然后在继续转动，并可重复抽取